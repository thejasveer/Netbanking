{
  "name": "bank-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cross-env NODE_ENV=production  npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "start": " node dist/index.js",
   
    "watch": "cross-env NODE_ENV=development  npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js --watch",
    "dev": "npm run watch && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@repo/db": "*",
    "@trpc/client": "^11.0.0-rc.403",
    "@trpc/server": "^11.0.0-rc.403",
    "@types/express": "^4.17.21",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "esbuild": "^0.20.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
